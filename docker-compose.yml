services:
  web:
    build: .
    container_name: data-to-db
    ports:
      - "6767:6767" # Data upload server
    volumes:
      - ./app/data:/app/data
      - ./app/templates:/app/templates
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
